// Replacer, which replaces JSX-like sytnax with HTML
htmx(string, {
  card: string | (attrs: [name: string, value: string]) => string
})

RewritingStream: https://github.com/inikulin/parse5/blob/master/packages/parse5-html-rewriting-stream/docs/index.md

default export

htmx([
  'index.html',
  'about.html'
], {
  card // Imports from outside, always a function
  avatar
})
